name: Message Queue Nuget CI
on:
  # run this workflow manually.
  workflow_dispatch:
jobs:

  build_push_Message_Queue:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0'

      - name: Restore Dependencies
        run: |
          cd ./Backend/dotnet/IOT/IOTConcept
          dotnet restore

      - name: Build and Pack
        run: |
          cd ./Backend/dotnet/IOT/IOTConcept
          dotnet pack --configuration Release

      - name: Push NuGet Package
        env:
          NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
        run: |
          cd ./Backend/dotnet/IOT/IOTConcept/MessageQueue
          dotnet nuget push ./Dkeshri.MessageQueue/bin/Release/*.nupkg \
            --api-key $NUGET_API_KEY \
            --source https://api.nuget.org/v3/index.json; \
          dotnet nuget push ./Dkeshri.MessageQueue.RabbitMq/bin/Release/*.nupkg \
            --api-key $NUGET_API_KEY \
            --source https://api.nuget.org/v3/index.json


